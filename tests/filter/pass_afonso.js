let arr2 = arr1.filter((x) => x % 2 === 0)
let arr3 = arr1.filter((x) => x % 3 === 0)
let arr4 = arr1.filter((x) => (x % 3 === 0) & (x % 2 === 0))
let arrRandom2 = arr1.filter((x) => (x % 3 === 0) & (x % 2 === 0))
