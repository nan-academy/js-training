{
  "private": true,
  "index": "primitives.js",
  "scripts": {
    "update": "git reset && git stash && git pull https://github.com/nan-ci/js.git master && git stash pop",
    "test": "./tester"
  }
}
